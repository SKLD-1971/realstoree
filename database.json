{
  "settings": {
    "site_name": "CmdStore"
  },
  "users": [
    {
      "id": 1,
      "username": "devadmin",
      "password": "password123",
      "email": "admin@store.com",
      "role": "dev",
      "balance": 100,
      "dls_balance": 1000,
      "wl_balance": 0,
      "isBanned": false,
      "cart": [],
      "creationDate": "2024-01-01T00:00:00.000Z"
    }
  ],
  "orders": [],
  "announcements": [
    {
      "id": 1,
      "content": "Welcome to the new store! All services are online.",
      "active": true
    }
  ]
}```

#### 3. File: `api/get-database.js`
*(This file goes inside the `api` folder)*

This code gets your data. The path `../database.json` is correct because it goes up one level from `api` to find `database.json`.

```javascript
import { kv } from '@vercel/kv';
import initialDb from '../database.json';

export default async function handler(request, response) {
  try {
    let dbData = await kv.get('database');

    if (!dbData) {
      await kv.set('database', initialDb);
      dbData = initialDb;
    }

    return response.status(200).json(dbData);
  } catch (error) {
    return response.status(500).json({ message: 'Error fetching data.' });
  }
}
